if (typeof window !== "undefined") {
  if (typeof window.ratel === "undefined") {
    window.ratel = {};
  }

  window.ratel.{{crate_name}} = {
    initialize: (_conf) => {
      console.debug("Initializing {{crate_name}} with config");
    },
  };
}
