// Controller hooks live here. Each admin page that mutates server
// state goes through a `UseFactFoldAdmin*` hook (see
// `conventions/hooks-and-actions.md`). Stubs for now; populated as
// each page lands.
