// Placeholder for the meet action's web-side components. The feat(meet-action)
// scaffolding wired up `#[cfg(not(feature = "server"))] pub mod components` in
// `meet/mod.rs` but the directory was never created, breaking the web build.
// This stub keeps the module tree resolvable until the meet UI is implemented.
