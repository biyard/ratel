import { useController } from './use-controller';

export function Requirements() {
  // TODO: use or define hooks
  const _ctrl = useController();

  return (
    <>
      <div>Requirements</div>
    </>
  );
}
